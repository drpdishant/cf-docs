image: node:14
stages:
- test

test:
 tags: 
    - other-runner
 image: node:14
 services:
    - docker:dind # This allows us to use docker commands
 stage: test
 script: 
     - node -v
     - ls
     - ll
     - cf --version
